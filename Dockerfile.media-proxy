FROM node:20-alpine

WORKDIR /app

# The proxy server only uses Node built-ins, no npm install needed.
COPY server/mediaProxyServer.mjs ./server/mediaProxyServer.mjs

ENV MEDIA_PROXY_PORT=8787

EXPOSE 8787

CMD ["node", "server/mediaProxyServer.mjs"]

